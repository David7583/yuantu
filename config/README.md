# config

Runtime configuration root. Real config files live here but are NOT committed — see /config.example for templates.

Intentionally empty in the repository; populated at runtime by the pipeline.
